node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.*
.DS_Store
*.log
tests/test-config.yaml

# Compiled JS files in src (should be in dist)
src/**/*.js
src/**/*.js.map
# Exception: test-helper.js is an intentional JS file (not compiled from TS)
!src/__tests__/helpers/test-helper.js

coverage/**

# Session and lock files - keep directories but ignore contents
.sessions/*
!.sessions/.keep

.locks/*
!.locks/.keep
mcp-abap-adt-adt-clients-*.tgz
sk.json
src/__tests__/helpers/test-config.yaml
*.env

# Claude Code local settings
.claude/

# Codex CLI
.codex/

# Temp/scratch files
cookies.txt
*.abap

# Credential/config files (not for repo)
mdd-*.json

# Subagent-driven development scratch (ledger, briefs, review packages)
.superpowers/

# ATC probe evidence (raw captures; the curated ones live in docs/evidence)
atc-probe*/
atc-e19*/
abapunit-e19*/
abapunit-e77*/
validation-probe*/
probe-e19-logs/
shared-setup-verdict.json
.jest-shared-session.json
profiler-probe/

# Probe output: raw ADT answers captured by integration tests, per run.
refusal-visibility-evidence.json
