# Operation Delays Summary

- Delays are configured in `test-config.yaml` under `test_settings.operation_delays`.
- `BaseTester` applies delays automatically for create/update/activate/delete flows.
- Only add manual delays when bypassing `BaseTester`.
