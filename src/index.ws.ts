/**
 * ADT Clients — WebSocket barrel
 * Covers: AdtClientsWS. Contract types come from
 * @mcp-abap-adt/interfaces — that is the one place to import them.
 */

export { AdtClientsWS } from './clients/AdtClientsWS';
